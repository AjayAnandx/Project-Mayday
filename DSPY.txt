Full Plan: DSPy Report Synthesis (Module A) + RL Multi-Hop Research Agent (Module B)
Both modules share one spine — a DSPy bridge layer, a deterministic metrics package, and a local-first LM policy — and both are flag-gated, additive, and revertible. Nothing in research_store.py, report_generator.py, function_registry.py, or the chat engine is modified; DSPy modules wrap them.
0. Shared Infrastructure (prerequisite for both)
0.1 New package backend/dspy/
File	Purpose
backend/dspy/bridge.py	try: import dspy → DspyBridge singleton; is_enabled() gate; kills itself on any import/init error; creates dspy.LM from config
backend/dspy/metrics.py	All deterministic metric functions (shared by both modules + compile + eval)
backend/dspy/compiler.py	Offline compile script (BootstrapFewShot → later GEPA) + PII gate + artifact save/load
backend/dspy/evaluate.py	CLI eval harness: runs dev set through module vs. current baseline, prints A/B table
backend/dspy/artifacts/	Compiled prompts/demos (gitignored; scanned by PII gate before serving)
backend/dspy/__init__.py	Exports modules; no-op when disabled
0.2 Config (config.yaml — additive section)
dspy:
  enabled: false            # master kill-switch
  research_synthesis: false # Module A
  research_agent: false     # Module B
  model: ""                 # empty = reuse ollama.model/endpoint (local-first)
  reflection_model: ""      # empty = local; set to cloud URI = opt-in delegation
  max_hops: 12
  max_iters_report_sections: 2   # ChainOfThought depth for synthesis
  artifacts_dir: dspy_artifacts
0.3 LM policy (privacy, from earlier analysis)
- Task model: Ollama (local) — research data never leaves the machine except via the already-existing model path.
- Reflection model: local by default; cloud only when reflection_model set AND traces de-identified (content fields stripped; only tool calls + structured store data remain).
- All bootstrapped demos pass a PII gate (email/phone/name-from-graph regex scan) before artifacts are saved or served.
0.4 Dev set builder
- Source: completed research topics in the Mayday Research folder, their linked conversations, and operations/ log entries.
- Trace shape per topic: research_questions → [hop actions + args + results] → data_points/entities/findings → report → scores.
- Target: 30–80 gold topics (enough for BootstrapFewShot; GEPA scales with more).
Module A — DSPy Report Synthesis + Summary Distillation
Goal
Replace template-dump reports with store-faithful synthesis: executive summary, answered research questions, and narrative sections — while the deterministic template remains the fallback and the integrity guard stays in force.
A.1 Signatures (in backend/dspy/research_synthesis.py)
class ExecutiveSummary(dspy.Signature):
    """Synthesize an executive summary from structured research data."""
    research_questions: list[str]   = dspy.InputField()
    data_points: list[dict]         = dspy.InputField()  # {label,value,unit,confidence,sources}
    entities: list[dict]            = dspy.InputField()
    findings: list[dict]            = dspy.InputField()
    executive_summary: str          = dspy.OutputField(desc="3-5 sentences; every number cited must come from the inputs")

class AnswerResearchQuestions(dspy.Signature):
    """Answer each research question using only the provided data; mark gaps."""
    research_questions: list[str] = dspy.InputField()
    data_points: list[dict]       = dspy.InputField()
    findings: list[dict]          = dspy.InputField()
    answers: list[dict]           = dspy.OutputField(desc="[{'question':..., 'answer':..., 'confidence':..., 'answered': bool}]")

class SynthesisNarrative(dspy.Signature):
    """Narrative synthesis per section (key entities / data points / findings)."""
    topic: str                 = dspy.InputField()
    section: str               = dspy.InputField(desc="Literal[...] of 'entities'|'data_points'|'findings'")
    inputs: list[dict]         = dspy.InputField()
    narrative: str             = dspy.OutputField(desc="2-4 paragraphs, grounded strictly in inputs")
- ExecutiveSummary writes back into the store via existing update_summary(topic, summary) (research_store.py:572) — no schema change.
- Report builder: new build_dspy_md(projects, title) in a thin adapter (kept out of report_generator.py — new file backend/dspy/report_builder.py), composing: summary → answered-questions → template tables (reused verbatim from _build_section_md) → synthesis narratives → existing appendix. generate_report / generate_combined_report in report_generator.py get an optional synthesis: bool param that routes to DSPy when enabled, else today's template (report_generator.py:137).
A.2 Integrity guard (deterministic, always on even when DSPy is off)
- Post-check on every synthesized artifact: every numeric token in summary/answers/narrative must appear in store data_points values; every entity name must exist in store entities; every cited source ∈ store sources. Failure → drop the section, fall back to template for that section only, log metric miss.
A.3 Metrics (in metrics.py, all LM-free)
- fabrication_score: numbers/entities/sources ∈ store (the guard above, as a 0–1 score).
- coverage_score: fraction of research_questions with answered=true and ≥1 supporting data point.
- summary_faithfulness: summary length bounds + required-entity coverage (entities in store appearing in summary).
- semantic_quality (report-only, never used for demo selection): dspy.SemanticF1 against the previous template report — used in eval output, excluded from compile.
A.4 Compile flow
1. BootstrapFewShot with 4 bootstrapped demos (16 labeled default) from dev set gold summaries/answers.
2. Later (Phase 5): GEPA with Prediction(score=combined_metrics, feedback=... ); reflection LM local by default.
3. compiler.py runs dspy.compile(program, trainset=...), PII-gates the artifact, saves to dspy_artifacts/report_synthesis/.
4. Runtime loads artifact; no LM calls at startup.
A.5 Runtime flow (research_functions path)
generate_report(topic, fmt="md", synthesis=None)
  → synthesis = cfg.dspy.research_synthesis and bridge.is_enabled()
  → if synthesis: build_dspy_md(...) → integrity guard → on any section fail: template fallback
  → existing md/pdf/chart paths unchanged
Module B — RL Multi-Hop Research Agent + Gap Detection
Goal
A goal-directed loop: plan → search → extract → store → verify gaps → done. Replaces the hand-written 3-step instruction block (chat.py:324–344) without touching the chat engine.
B.1 Design choice (Mode A — self-contained tool)
research_agent is a single LLM tool added to the registry. The chat engine's existing iterative loop calls it once; the agent runs its own bounded hop loop internally and streams progress back as tool_call bubbles. The 20-iteration chat loop, duplicate guard, tools=[] rule (chat.py:916) stay untouched. Mode B (per-hop integration into the chat loop) is explicitly rejected for v1.
B.2 Module (backend/dspy/research_agent.py)
class ResearchHop(dspy.Signature):
    """Decide the next research action given current state. Actions are typed."""
    topic: str                    = dspy.InputField()
    research_type: str            = dspy.InputField()
    research_questions: list[str] = dspy.InputField()
    task_title: str               = dspy.InputField()          # current task from store
    collected: list[dict]         = dspy.InputField(desc="data_points+entities+findings so far")
    last_observation: str         = dspy.InputField(desc="result of the previous action or ''")
    score: float                  = dspy.InputField(desc="current deterministic gap score 0..1")
    feedback: str                 = dspy.InputField(desc="GEPA-style gap feedback from checker")
    next_action: Literal["web_search_and_fetch","extract_data_from_sources","batch_add_data_points","add_finding","finish"]
    args: dict                    = dspy.OutputField(desc="valid args for next_action; finish => {}")

class GapChecker(dspy.Signature):
    """Score research progress and produce actionable gap feedback (GEPA Prediction pattern)."""
    research_questions: list[str] = dspy.InputField()
    task_title: str               = dspy.InputField()
    collected: list[dict]         = dspy.InputField()
    score: float                  = dspy.OutputField(desc="0..1, deterministic metric verdict")
    feedback: str                 = dspy.OutputField(desc="unanswered questions, low-confidence findings, missing sources")
- Hops call the existing functions (web_research.py wrappers: web_search_and_fetch, extract_data_from_sources, batch_add_data_points) through dispatch_call — same arg-repair safety net, same operation-log recording, zero new execution paths.
- Budget: max_hops=12; finish when score ≥ 0.9, next_action=finish, or budget exhausted (then finish regardless).
- After finish: auto generate_report(synthesis=on) + optional promote if user asked.
B.3 Deterministic reward (the RL signal — metrics.py)
Per-hop reward, summable for rollout score:
- +1 valid action (passes dispatch validation) — the Literal field makes invalid names impossible by construction
- +1 per new data point with ≥1 source; +0.5 extra if confidence=high AND ≥2 sources
- +1 per research question newly covered (keyword overlap between question terms and new findings/data-point labels)
- −1 repeated identical hop (dedup — mirrors the chat engine's 3× duplicate guard)
- 0 otherwise
Rollout score = normalized sum → feeds GapChecker.score → next hop sees it.
B.4 Runtime loop (inside research_agent tool)
current_task = research_store.get_active_task(topic)   # uses existing depends_on chain
state = collect(store fields) ; score, feedback = metrics.initial(topic)
for hop in range(max_hops):
    action, args = program(topic, type, questions, task, state, last_obs, score, feedback)
    if action == "finish": break
    result = dispatch_call(action, args)               # existing registry + repair
    state = collect(store fields)                      # store mutated by batch_add_data_points etc.
    score, feedback = GapChecker(...)                  # deterministic metrics + feedback
    stream progress via tool_call bubble
if score < 0.5: log weak-spot feedback to research note (gap_review.md)
generate_report(synthesis=Module A)
B.5 Compile flow
1. BootstrapFewShot: demos = successful research topics' hop traces from dev set (de-identified).
2. Later: GEPA with Prediction(score=rollout_score, feedback=GapChecker.feedback) — this is the RL loop at compile time (reflective evolution of the hop instructions); local reflection LM by default.
3. dspy.History optional: past hops passed as an input field instead of growing message history, keeping prefill small.
Evaluation & A/B gate (both modules)
evaluate.py (CLI, offline):
- Dev set (30–80 topics) run twice: baseline (current template report + hand-written 3-step prompt) vs DSPy.
- Table: token count, fabrication_score, coverage_score, tool-call validity, rollout score, latency, SemanticF1 (report-only metric).
- Flags flip only when DSPy ≥ baseline on all deterministic metrics (SemanticF1 may lag; it's informational).
Test plan (follows test_research_store.py conventions — mocked store/config/op-log, zero repo pollution)
- backend/test_dspy_metrics.py — each metric: known-good/known-bad fixtures, fabrication guard catches hallucinated numbers.
- backend/test_dspy_bridge.py — import-failure fallback, disabled flags no-op, artifact load/save, PII gate rejects seeded PII.
- backend/test_dspy_synthesis.py — integrity guard drops bad section → template fallback; summary written via update_summary; combined report path.
- backend/test_dspy_agent.py — mocked dispatch_call: hop loop terminates on finish/budget, dedup penalty fires, gap checker feedback drives a second search hop, invalid action impossible by construction.
- Regression: existing test_research_store.py + test_data_pipeline.py + test_dispatch_repair.py must pass untouched.
Build order & effort
Phase	Work
R0	dspy/ package + bridge + config + spike: dspy.LM against Ollama endpoint
R1	metrics.py + dev-set builder + evaluate.py harness (baseline only)
R2	Module A synthesis + integrity guard + flag
R3	Module B agent + research_agent tool + flags
R4	BootstrapFewShot compile both modules; PII gate on artifacts
R5	GEPA compile (offline, local reflection) + A/B flip
What is deliberately NOT touched
- research_store.py schema/API, report_generator.py template + chart logic, function_registry.py dispatch/repair, chat.py engine loop, config.yaml existing keys, hardcoded RESEARCH_TYPE_TASKS (task planner is a later optional phase), tool schemas.
- DSPy never touches conversation/chat prompts — scoped to research report + research-agent boundaries only.
Open question before execution: for Module B's research_agent, do you want it exposed as an LLM tool (Mode A — engine stays pristine, agent runs internally) or do you prefer it wired into the existing chat tool loop per-hop? I recommend Mode A.




Full Plan: Mayday User Awareness + Personal Task Store + DSPy Module C
0. Vision
Mayday starts knowing nothing about the user. Over time it builds a living world model — favorites, belongings, friends, problems, goals — through passive observation + a few daily questions. It manages all personal tasks (todos, calendar, reminders, notes) through one unified layer, and every task action passes through an awareness decision gate: Mayday only assumes, creates, reminds, and suggests according to what it actually understands — asking when uncertain, warning on contradictions, blocking when trust is insufficient. DSPy compiles each learned behavior (extraction, scoring, asking, reflecting, gating, snapshot selection) from Mayday's own usage history, so the system improves from usage data instead of hand-edited prompts.
1. Locked decisions
Decision	Choice
Learning mode	Passive + active onboarding (2-3 targeted questions/day)
Gate strictness	Ask / warn / block
Awareness storage	Dual store: user_profile.json + synced belief nodes in memory_graph.json
DSPy posture	Flag-gated default-off, local-first LM policy, deterministic metrics only, typed signatures
Privacy	All local; T1 (sensitive) never leaves machine, T2 de-identified only; consent tiers T0-T2 per belief
2. Architecture
OBSERVATIONS                        BELIEFS                             ACTIONS
────────────────                    ─────────────────                   ──────────────────
conversations/  ─┐                                        todos, events, notes,
operations/     ─┤ → Observer → Comprehension → Awareness  reminders, projects,
todos/events    ─┘   (DSPy UA-1)  (DSPy UA-2)    Store      research
daily activity  ────────────────────────────┐   user_profile.json + graph   │
                                            │          │   │               ▼
                            Reflection (DSPy UA-4)      ▼   ▼        DECISION GATE
                            Forgetting (decay)   World Snapshot ──►  (DSPy UA-5)
                            Validation (DSPy UA-3)  (DSPy UA-6)     allow|ask|warn|block
                                                                          │
                                                       PTS facade ────────┘
                                                       todos+events+notes+reminders
                                                       (DSPy PTS-1 task interpreter)
        OFFLINE: bootstrap → evaluate → compile (overnight job, local LM)
        user accept/reject + confirm/correct become next training labels
3. Part A — User Awareness Engine
A1. Awareness Store — backend/core/user_awareness.py
Ontology slots (each slot = set of beliefs):
identity     name, role, work/study, routine, timezone/location
relations    people: type (family/friend/colleague), closeness, contact freq, linked events
favorites    foods, music, movies, sports, places, brands (per domain)
belongings   pets, vehicles, devices, assets (+ care needs)
goals        short-term (exams, deadlines) + long-term (career, health)
problems     stress, health issues, financial constraints, worries
context      phase of life, schedule pattern (class days, commute)
state        transient mood/availability (fast decay)
Belief record: {id, slot, value, confidence, provenance, consent_tier, last_verified, observed_count, created_at, source_refs[]}
Dual-store sync: write user_profile.json (gate queries, O(1) slot lookups) → mirror as graph nodes belief:{slot}:{value} linked to user:Mayday-user, person nodes for relations (reuse add_edge_if_missing, tombstones, label normalization). Brain tab visualizes the user's world.
REST: GET/PUT /api/awareness, GET /api/awareness/snapshot, POST /api/awareness/import (batch from old conversations).
A2. Learning loop
1. Observer — hooks: conversation save, todo/event CRUD, op-log record, reminder fire. Raw text → candidate beliefs.
2. Comprehension + confidence — classify into slot; score by provenance: explicit = 0.9, repeated 3× = 0.75, single inference = 0.55, stereotype = 0.3. (Heuristic baseline; DSPy UA-2 replaces it once compiled.)
3. Validation — low-confidence belief + action depends on it → ask_user tool, max 2-3/day (onboarding queue); explicit answer → 0.95.
4. Reflection (weekly, Scheduler job) — consolidate duplicates, resolve contradictions (newer wins + provenance record), derive higher-order traits, monthly "world snapshot" summary.
5. Forgetting — confidence *= e^(-λt) on unused beliefs; < 0.3 → "unverified"; contradictions supersede; "don't mention X" → tombstone (reuse tombstone system).
6. Privacy — consent tier per belief; T1 = health/finances/relationships (never leaves machine); T0 = harmless facts.
A3. Decision gate — can_do(action, text, entities, context)
→ unknown entity in text (no belief)      → ask      + targeted question
→ contradicts known belief                → warn     + explanation
→ known, high confidence, safe            → allow    + enrich (link entities, attach reminder ideas)
→ sensitive slot + tier < required        → block    + reason (never overridden by DSPy)
Wraps: create_todo, create_event, create_reminder, create_personal_note, add_project_task (task text), create_research. Verdict returned to LLM tool layer + surfaced in frontend (yellow banner / modal, reuse ReminderDialog/Toast patterns).
World Snapshot injection — each engine turn injects compressed top-N beliefs (by confidence × recency × relevance) instead of raw memories; keeps context small.
4. Part B — Personal Task Store (PTS)
- New personal notes store — notes.json (standalone personal notes don't exist today; only project/research notes) + 3 tools: create_personal_note, list_personal_notes, search_personal_notes + REST endpoints.
- Unified facade — {id, kind: todo|event|note|reminder, text, time, entities[], source}; one query answers "what's on my plate" across all four stores.
- Entity linking — task entities auto-linked to beliefs (dad → person node, dog → belonging): enables contextual reminders ("call dad on his birthday" works because gate knows the birthday) and gate enrichment.
- Proactivity (Projection) — from snapshot + tasks, Mayday surfaces suggestions (renewal reminders, "exam in 3 days — study plan?"), each suggestion logged → becomes DSPy training signal.
5. Part C — DSPy Module C (the compiled brain)
C0. Shared infra (reuse from plan.md:5680 DSPy plan)
backend/dspy/: bridge.py (Ollama OpenAI-compatible bridge, local-first), metrics.py (deterministic metrics only), compiler.py (BootstrapFewShot → MIPROv2 → GEPA ladder), evaluate.py (A/B vs heuristic baseline), artifacts/ (compiled programs + eval reports). Config: dspy: section, modules.enabled: [], all default-off.
C1. Module table — 7 insertion points
ID	Replaces	Signature	Optimizer	Metric (deterministic)
UA-1	prose memory instructions	BeliefExtractor(text, slot_hint?) → beliefs[Belief]	BootstrapFewShot	exact slot/value match vs validated store; malformed-field rate
UA-2	hardcoded 0.9/0.75/0.55/0.3	ConfidenceAssessor(belief, evidence) → score	BootstrapFewShotWithRandomSearch	correlation with later user confirm/correct
UA-3	hand-written "ask 2-3/day" rule	AskPolicy(context, beliefs, action) → {ask, question}	BootstrapFewShot	info gained per interruption (confirm/correct rate); question-rejection rate
UA-4	hand-written consolidation	Reflector(month_beliefs) → {consolidated, contradictions, derived_traits}	MIPROv2	duplicate/contradiction count after; trait acceptance (no user override in next 7d)
UA-5 ⭐	hand-coded verdict heuristics	GatePolicy(action, text, entities, beliefs) → {verdict, reason, suggestion}	MIPROv2 → GEPA	user override rate per verdict class; false-block rate; allow-safety (no block of genuine constraint)
UA-6	naive top-N snapshot	SnapshotSelector(context, beliefs) → top_k	BootstrapFewShotWithRandomSearch	next-turn usefulness (offline LM judge on dev set only)
PTS-1	prompt-prose parsing	TaskInterpreter(text) → {kind, time, entities[]}	BootstrapFewShot	parse exact-match vs stored todo/event; entity-link hit rate
C2. Self-improving loop
user accepts/overrides a gate verdict, confirms/corrects a belief
        └──► labeled example appended to dev/train set
        └──► overnight compile job (local LM, T1-safe)
        └──► eval vs heuristic baseline; ship artifact only if metric improves
C3. What stays deterministic (never DSPy)
- user_profile.json + graph storage, decay math, tombstone rules
- Hard blocks (consent-tier violations) — DSPy feeds the gate, never overrides safety
- Observation hooks, scheduler, persistence
C4. Privacy posture
- Compile + eval run against local Ollama model only (beliefs are T1/T2); cloud model (gemma4:31b-cloud) inference allowed for T0 beliefs only
- Compile jobs: no conversation text leaves conversations/; eval reports store metrics only
6. Research papers (reading order, mapped)
Paper	Maps to
Generative Agents (Park 2023)	reflection + retrieval design (UA-4, UA-6)
MemGPT (Packer 2023)	belief store vs context split
MemoryBank (Zhang 2023)	forgetting curve
Survey: Memory in LLM Agents (2024)	taxonomy, gap check
Mem0 (2025)	production ADD/UPDATE/DELETE ops
Endsley SA (1995)	perception/comprehension/projection framing
Rich (1979) stereotypes	cold-start 0.3 beliefs
Kobsa UMS survey	belief maintenance, assumptions vs facts
Thrun & Mitchell (1995) lifelong learning	learn-without-forgetting tension
NELL (Mitchell 2018)	perpetual extraction + confidence
ToM in LLMs (Kosinski 2023 + Ullman critique)	relations/problems understanding
7. Config additions (config.yaml)
awareness:
  enabled: false
  max_daily_questions: 3
  decay_lambda: 0.02
  reflection_day: "mon"
dspy:
  enabled: false
  task_model: ""              # empty → ollama.model
  reflection_model: ""        # local model recommended
  modules: []                 # e.g. ["ua5_gate", "ua1_extractor"]
personal_notes:
  storage_path: notes.json
8. Files
New: backend/core/user_awareness.py (store+gate), backend/core/personal_notes.py, backend/core/awareness_observer.py, backend/dspy/module_c/*.py (7 modules), backend/api/awareness.py, backend/api/notes.py, tests (test_user_awareness.py, test_decision_gate.py, test_personal_notes.py, test_dspy_ua_modules.py), frontend AwarenessPanel.tsx (optional: belief browser in Brain tab).
Modified: chat.py (snapshot injection, gate wrap in engine, ask_user tool, DSPy module dispatch), function_registry.py (+8 tools: ask_user, list_beliefs, update_belief, create/list/search_personal_note, run_reflection, awareness_summary), data_store.py/api/todos.py/events.py (gate hook), main.py (reflection job), knowledge_graph.py (belief node type), scheduler.py (daily onboarding queue + weekly reflection), config.py, requirements.txt (dspy-ai), plan.md.
9. Phases & rollout
Phase	Content	DSPy
0	Awareness store + ontology + REST + graph sync + belief CRUD tests	—
1	Observer hooks + comprehension (heuristic confidence) + ask_user/list_beliefs/update_belief + snapshot injection	—
2	Reflection job + forgetting + tombstones	—
3	PTS: notes store + 3 tools + facade + entity linking	—
4	Decision gate wrap on todo/event/note/reminder/task tools + ask/warn/block frontend UX	UA-5 gate pilot (train on op-log, A/B vs heuristics, flag-gated)
5	Onboarding interview mode + consent tiers + docs	—
6	DSPy Module C expansion: UA-1/2/3/4/6 + PTS-1 compiled, overnight compile job	all modules, local LM
7	Self-improving loop hardening + eval dashboards	continuous

10. Testing (continued)
- Gate regression table — curated scenario matrix (30+ cases): unknown entity, contradiction, sensitive slot, low confidence, follow-up edit → expected verdict; gate must never silently flip on recompile
- DSPy eval — held-out months (e.g., train Jan-Jun, eval Jul): metric table per module vs heuristic baseline; artifact ships only if strictly better; dspy.Evaluate reports with sample failure cases
- Onboarding QA — question quality: no duplicate questions, ≤3/day, correct slot after answer (confidence 0.95), question bank exhaustion → fall back to passive
- PTS integration — unified query returns merged todo/event/note/reminder; entity links resolve through gate enrichment; reminder with linked entity fires with contextual text
- Privacy tests — T1 beliefs excluded from any cloud-model path; snapshot injection strips T1 when task model is cloud; compile logs contain no conversation text
- Manual — Brain tab belief visualization, frontend ask/warn/block UI, weekly reflection output, "call dad on his birthday" end-to-end, DSPy flag on/off parity
11. Risks & mitigations
Risk	Mitigation
Wrong beliefs persist / confidence inflates	Decay + contradiction supersede + explicit confirm path; DSPy UA-2 learned scorer over hardcoded values
Gate blocks too much (annoyance)	Ask/warn default; block reserved for consent violations; override rate is the compile metric
Gate too permissive (wrong assumptions)	Unknown-entity → ask; every allow is logged; weekly review of allow-with-low-confidence
DSPy compile cost / flaky metrics	Deterministic labels only (user confirm/correct/override); small typed modules; local LM; artifacts gated by eval
Context bloat from snapshot	UA-6 selects top-k (token budget constant); injection section capped (e.g. 1.5k chars)
Privacy leak via cloud model	T1 never in cloud path; task model check at injection; consent tiers enforced in observer
Over-interviewing user	Max 3/day, question bank dedup, streak guard, "later" snooze
Existing 212-test suite regression	Gate wraps tools behind flag awareness.enabled; default-off until Phase 4; all phases additive
12. Estimate
Phase
0: Store + ontology + REST + graph sync
1: Observer + comprehension + memory tools + snapshot
2: Reflection + forgetting
3: PTS (notes + facade + linking)
4: Decision gate + UX + UA-5 pilot
5: Onboarding + consent + docs
6: DSPy Module C full compile (UA-1/2/3/4/6, PTS-1)
7: Self-improving loop hardening
Total
13. Open items before implementation
1. DSPy version pin — dspy-ai 2.5.x vs 3.x (API differs; verify Ollama OpenAI-compatible bridge works with the pinned version on this machine first — Spike: 1-2 hrs, Phase 0)
2. Question bank — initial 40-60 onboarding questions curated across slots (Phase 5 deliverable; heuristics ship with 15 basics in Phase 1)
3. Snapshot cap — exact char budget for World Snapshot (default proposal: 1.5k chars, UA-6 tunes it)
4. Frontend surface — AwarenessPanel inside Brain tab (belief browser + confidence slider + "correct this" affordance) vs minimal chat-only correction (recommend chat-only for Phase 4, panel in Phase 7)
5. Where to write this plan — plan.md as "Mayday User Awareness + Personal Task Store + DSPy Module C — Implementation Plan" (single section, cross-referenced from the DSPy section at plan.md:5680 and the Telegram plan)